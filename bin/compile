#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Installing youtube-dl"

BUILD_DIR=$1
VENDOR_DIR="vendor"

cd BUILD_DIR
cd VENDOR_DIR

curl https://yt-dl.org/latest/youtube-dl
chmod a+rx /usr/local/bin/youtube-dl
